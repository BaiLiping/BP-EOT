This repository contains Python code that was translated from MATLAB.

All Python files should be formatted using the `black` code formatter.

To install black, run:
`pip install black`

To format a file, run:
`black <file_name>.py`
